# ASEM Talint Skeleton

This repository now includes a minimal ASEM-centered application skeleton for UM Hackathon Domain 2. The current vertical slice evaluates a candidate against a semiconductor training track, computes a deterministic suitability breakdown, and sends that evidence to Z.AI GLM for a structured recommendation and explanation.

## Primary User

The primary user is ASEM as a training-provider and talent-pipeline operator. The current API is designed to help ASEM decide whether a candidate is ready for a semiconductor-aligned pathway and which gaps should be addressed next.

## Current Scope

- typed request and response schemas for candidate-track fit decisions
- deterministic score breakdown for readiness, gaps, and supporting factors
- optional structured resume context for skills, projects, internships, certifications, and tools
- resume upload endpoint for PDF and DOCX files with size limits, OCR fallback for image-only PDFs, and PII-redacted preview output
- `LLMProvider` abstraction with a primary `ZAIProvider` and an optional `ILMUProvider` compatibility path
- one FastAPI endpoint for candidate-track fit decisions
- browser dashboard at `/` and `/dashboard`
- dedicated dashboard resume-intake section that applies parsed resume evidence into the payload automatically
- clearly labeled degraded local demo mode for dashboard validation
- fixture-backed official-data ingestion slices for wages, semiconductor hotspots, and employer-demand signals
- deterministic GIS accessibility, OJT matching, and wage-mobility endpoints
- focused unit and integration tests for the decision slice

## Architecture Summary

- `src/asem_talent/domain`: typed models and deterministic scoring
- `src/asem_talent/llm`: prompt builder, provider interface, Z.AI provider, and optional ILMU compatibility provider
- `src/asem_talent/services`: decision engine orchestration
- `src/asem_talent/api`: FastAPI app, dependencies, and routes
- `tests`: unit and integration coverage for the initial flow

Z.AI GLM remains central in the reasoning path. Deterministic scoring stays in code, and the model is used for structured recommendation and explanation output. The ILMU path is optional non-judge-path infrastructure only.

## Setup

1. Create and activate a Python 3.11+ environment.
2. Install dependencies with `pip install -e .[dev]`.
3. Copy `.env.example` to `.env` and set `ZAI_API_KEY` plus a valid `GLM_MODEL` available to your account.
	If you are using Open.BigModel / Zhipu's OpenAI-compatible endpoint, set `ZAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4` and a model such as `GLM_MODEL=glm-4`.
4. If optional organizer-approved ILMU infrastructure is available, set `ILMU_API_KEY`, `ILMU_BASE_URL`, and `ILMU_MODEL` as well.
5. Start the API with `uvicorn asem_talent.app:app --reload`.
6. Open `http://127.0.0.1:8000/` for the dashboard.

## Preliminary Round Submission Pack

For the first round or preliminary round, the team submission should package these artifacts:

- Product Requirement Document (PRD) in PDF format
- System Analysis Document (SAD) in PDF format
- code repository link or archive, with a public GitHub or source-code link encouraged by the handbook
- Quality Assurance Testing Document (QATD) in PDF format
- preliminary-round presentation pitch deck
- 10-minute pitching video with prototype demonstration in MP4 or MOV format

Across those artifacts, judges should be able to see the ideation, development approach, domain-problem fit, and product feasibility of the solution. In this repository, that means the PRD, SAD, QATD, pitch narrative, and demo should all keep the Z.AI GLM decision path explicit rather than describing the product as a generic chatbot.

References to external resources, datasets, articles, templates, benchmarks, or borrowed ideas used in the preliminary submission should be cited visibly. Submission must be made through the official UMHackathon website only. Each team has one submission attempt, and once the final submission is sent, no further changes are allowed.

The workspace already includes official sample templates and handbook files under `Domain 2 (Participants)/` for the PRD, SAD, QATD, and handbook-aligned packaging flow.

Working preliminary-round submission drafts now live under `submission/preliminary/`:

- `submission/preliminary/PRD.md`
- `submission/preliminary/SAD.md`
- `submission/preliminary/QATD.md`
- `submission/preliminary/pitch-deck-outline.md`
- `submission/preliminary/video-demo-runbook.md`
- `submission/preliminary/submission-checklist.md`

## Official Data Ingestion Jobs

The app still reads normalized CSVs from `data/official/`, but those files are now intended to be generated by ingestion jobs rather than hand-maintained.

Use the ingestion CLI like this:

`python -m asem_talent.data.ingest_jobs --wages-source <OpenDOSM CSV or URL> --employer-source <employer export CSV or URL> --hotspot-source data/source_registry/semicon_hotspots.registry.json`

For wages, the default source is now the official OpenDOSM formal-sector-wages dashboard page. The ingestion job extracts the embedded `__NEXT_DATA__` payload and normalizes the latest state-level median salary values into the CSV the app serves.

The job normalizes those sources into:

- `data/official/wages_formal_mfg.csv`
- `data/official/employer_demand_semicon.csv`
- `data/official/semicon_hotspots.csv`

You can also set these environment variables instead of passing command-line arguments:

- `OPENDOSM_WAGES_SOURCE`
- `EMPLOYER_DEMAND_SOURCE`
- `HOTSPOT_REGISTRY_SOURCE`
- `OPENDOSM_SOURCE_MANIFEST`

The discovered OpenDOSM dataset ids and storage URLs are tracked in `data/source_registry/opendosm.registry.json`.

## Environment Variables

- `APP_ENV`: application environment label
- `ZAI_API_KEY`: server-side Z.AI API key
- `ZAI_BASE_URL`: OpenAI-compatible GLM base URL, for example `https://api.z.ai/api/paas/v4` or `https://open.bigmodel.cn/api/paas/v4`
- `GLM_MODEL`: target GLM model identifier
- `ILMU_API_KEY`: optional ILMU API key for the non-judge-path compatibility route
- `ILMU_BASE_URL`: optional ILMU OpenAI-compatible base URL for the non-judge-path compatibility route
- `ILMU_MODEL`: optional ILMU model identifier for the non-judge-path compatibility route
- `ILMU_MAX_TOKENS`: optional ILMU completion-token cap; defaults to a higher budget than the Z.AI path because the tested ILMU route needed a compact but slightly larger JSON completion window
- `ILMU_NOTES_MAX_CHARS`: optional ILMU prompt-budget cap for free-text and resume-summary fields; defaults to a smaller window than the Z.AI path so the compact ILMU route can finish within its tested completion budget
- `ZAI_TIMEOUT_SECONDS`: outbound provider timeout
- `ZAI_MAX_TOKENS`: response token limit for structured decisions
- `ZAI_TEMPERATURE`: generation temperature for structured output
- `PROMPT_NOTES_MAX_CHARS`: limit for free-text notes included in prompts

## OpenAI-Compatible GLM Access

The runtime provider in this repository already uses an OpenAI-compatible chat-completions call through `httpx`, so no separate `zhipuai` SDK integration is required.

If you want to point the shipped GLM path at Open.BigModel / Zhipu, set:

- `ZAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4`
- `GLM_MODEL=glm-4`

The provider trims a trailing slash automatically and calls `/chat/completions` using the same structured JSON contract used elsewhere in the app.

ASSUMPTION: any ILMU route used with this repository exposes the same OpenAI-compatible `/chat/completions` contract. The ILMU path stays disabled until `ILMU_API_KEY`, `ILMU_BASE_URL`, and `ILMU_MODEL` are configured.

## Tests

Run focused validation with `pytest`.

## Demo Flow

1. Upload a resume through the dashboard intake section or call `POST /v1/resumes/parse` with a PDF or DOCX file.
2. The parser enforces a 2,000,000 byte limit, extracts text, uses OCR when a PDF does not expose enough direct text, redacts contact PII in the preview, and returns structured `resume_context` evidence.
3. Submit a candidate profile and target training track to `POST /v1/decisions/candidate-track-fit`.
	The candidate payload can optionally include `resume_context` with structured skills, project highlights, internships, certifications, and inferred role signals.
4. The service computes a deterministic suitability breakdown and skill gaps.
5. The decision engine merges explicit candidate fields with structured resume evidence, then sends bounded JSON evidence to Z.AI GLM.
6. The API returns a typed explanation object plus provider usage metadata.
7. If optional ILMU infrastructure is configured, the dashboard and `POST /v1/decisions/candidate-track-fit/ilmu` expose the same structured contract as a non-judge-path compatibility route.
8. For local UI validation, the dashboard can call `POST /v1/decisions/candidate-track-fit/demo`, which is explicitly labeled as degraded non-judge-path behavior.
9. Inspect signal slices at `GET /v1/signals/wages` and `GET /v1/signals/employer-demand`.
10. Evaluate accessibility with `POST /v1/signals/accessibility`, OJT options with `POST /v1/matching/ojt`, and wage mobility with `POST /v1/analysis/wage-mobility`.

## Official-Data Slices

The repository now includes local fixture slices under `data/official/` that are shaped for official-data ingestion work:

- `wages_formal_mfg.csv`: OpenDOSM-style manufacturing wage slice
- `employer_demand_semicon.csv`: employer-demand slice aligned to semiconductor-adjacent OJT roles
- `semicon_hotspots.csv`: hotspot slice for Cyberjaya, Penang, Kulim, and Johor

These files are generated development slices, not authoritative full exports. They keep the service runnable now while preserving a clean interface for swapping in real official downloads and employer exports.

## Compliance Note

Z.AI GLM remains the only runtime reasoning provider in the shipped judge path represented by this skeleton. The ILMU compatibility route is optional, disabled by default, and clearly labeled as non-judge-path behavior.

## KPI Framing

This initial slice supports a later ASEM dashboard by surfacing readiness signals, missing inputs, and pathway steps. Wage mobility, OJT allocation, GIS accessibility, and cohort KPI aggregation remain `UNSPECIFIED` for the next implementation slices.

## Limitations

- The current endpoint covers one candidate-to-track decision flow only.
- Resume upload now falls back to OCR for image-only PDFs, but handwritten content and very low-quality scans remain `UNSPECIFIED`.
- Resume parsing is heuristic and intended to bootstrap `resume_context`, not replace human review of extracted evidence.
- Official wages now ingest from the OpenDOSM formal-sector-wages dashboard payload, but employer-demand exports still depend on an external source file.
- Real ASEM internal data schemas remain `UNSPECIFIED`; current tests use synthetic fixtures.
- The dashboard currently focuses on one fit-review workflow rather than a full multi-page analytics suite.
- The current data layer uses official-shaped fixture slices rather than live downloads or full dataset refresh jobs.
